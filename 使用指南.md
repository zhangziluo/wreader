# wreader 使用指南（小白版）

> 这份文档假设你**从没用过命令行**。跟着一步一步复制粘贴，就能在终端里读上第一页。
> 全程大概 10 分钟，不需要写任何代码。

遇到看不懂的名词别慌，文末有[术语小词典](#术语小词典)。报错卡住了直接翻[报错急救表](#报错急救表)。

---

## 目录

- [这个软件能帮你做什么](#这个软件能帮你做什么)
- [第 1 步：确认电脑里有 Python](#第-1-步确认电脑里有-python)
- [第 2 步：把项目放到本地](#第-2-步把项目放到本地)
- [第 3 步：安装（一条命令）](#第-3-步安装一条命令)
- [第 4 步：准备你的书](#第-4-步准备你的书)
- [第 5 步：导入书库](#第-5-步导入书库)
- [第 6 步：读出第一页](#第-6-步读出第一页)
- [第 7 步：按场景学按键](#第-7-步按场景学按键)
- [第 8 步：看统计和成就](#第-8-步看统计和成就)
- [第 9 步：把 wreader 调成你喜欢的样子](#第-9-步把-wreader-调成你喜欢的样子)
- [第 10 步：换电脑或重装，把阅读记录带走](#第-10-步换电脑或重装把阅读记录带走)
- [关掉终端之后：下次怎么打开 werd](#关掉终端之后下次怎么打开-werd)
- [报错急救表](#报错急救表)
- [常用命令速查卡](#常用命令速查卡)
- [想卸载怎么办](#想卸载怎么办)
- [术语小词典](#术语小词典)

---

## 这个软件能帮你做什么

一句话：**它把终端变成一个读书器。**

具体一点，它解决这几个问题：

| 你遇到的麻烦 | wreader 怎么解决 |
| --- | --- |
| 下载的 txt 是乱码 | 自动识别编码，转成 UTF-8 存好，之后永远不乱 |
| 忘了读到哪 | 自动记住位置、书签，下次打开直接接着读 |
| 想看看自己有多自律 | 统计每天读了多久，一张 30 天热力图，还有 48 个成就可以解锁 |
| 不想开浏览器 / 不想装 App | 它只占终端，读书时没有弹窗没有广告，默认全程离线（只有一个可选的地理成就查询会联网，可以关掉） |
| 换了电脑 / 重装了系统 | `werd data export` 把时长、进度、书签、成就打包成一个 JSON 文件，新机器上 `werd data import` 合并回来（只加不减，见[第 10 步](#第-10-步换电脑或重装把阅读记录带走)） |

适合谁：喜欢在电脑前用键盘看书、想安安静静把一本小说读完的人。

---

## 第 1 步：确认电脑里有 Python

打开**终端**：

- **macOS**：按 `Command + 空格`，输入 `终端` 或 `Terminal`，回车。
- **Windows**：按 `Win` 键，输入 `PowerShell`，回车。
- **Linux**：你大概已经知道在哪了。

然后复制这一行粘进去，按回车：

```bash
python3 --version
```

- 看到 `Python 3.11.x` 或更高（3.12、3.13 都行）→ 太好了，进入第 2 步。
- 看到 `Python 3.9` / `3.10` → 版本太低，需要升级（见下）。
- 看到 `command not found` / `不是内部或外部命令` → 电脑里没装 Python。

**安装或升级 Python 的办法：**

- **macOS**：去 [python.org/downloads](https://www.python.org/downloads/) 下载最新版安装包，双击装完即可。
  （用 Homebrew 的话：`brew install python@3.13`）
- **Windows**：同样去 python.org 下载安装包，**安装第一步一定要勾上 `Add python.exe to PATH`**，
  否则终端里敲 `python` 会找不到。装好后 `python --version` 能出版本号就对了。
  如果 Windows 里 `python3` 没反应，试试 `python --version`。
- **Linux**：`sudo apt install python3 python3-pip python3-venv`（Debian/Ubuntu 系）。

> 小提示：Windows 里如果你敲 `python3` 报错，但 `python` 可以，那后面所有命令里的
> `python3` 都换成 `python` 就行。

---

## 第 2 步：把项目放到本地

**最省事的办法：用 `git clone` 下载。** 复制这一行到终端回车：

```bash
git clone https://github.com/zhangziluo/wreader.git
```

它会在你**当前所在的目录**（一般是主目录）建一个叫 `werd` 的文件夹。
如果提示 `command not found: git`，说明电脑没装 git：macOS 用 `xcode-select --install`
或 `brew install git`，Linux 用 `sudo apt install git`。

**没有 git 也行**：在 GitHub 页面上点 `Code` → `Download ZIP`，解压到一个你找得到的位置，
别放在中文路径里最保险。

接着**进入这个目录**（这一步很关键，后面所有命令都要在这里执行）：

```bash
cd wreader
```

`cd` 是 "change directory"（切换目录）的缩写，后面跟文件夹路径。
如果你是解压到别的位置，就换成那个路径，比如 `cd ~/Downloads/wreader`。
不想手打路径的话：先输入 `cd `（注意有个空格），然后把文件夹**拖进终端窗口**，路径会自动补上，再按回车。

**怎么知道成功了？** 输入 `ls` 回车，应该能看到 `pyproject.toml`、`werd`、`install.sh` 这些名字。

```bash
ls
# README.md  wreader  pyproject.toml  使用指南.md  install.sh
```

> ⚠️ 后面每开一个新终端窗口，都要重新 `cd` 一次到这个目录（除非你按
> [关掉终端之后：下次怎么打开 werd](#关掉终端之后下次怎么打开-werd) 配好了别名）。
> 忘了 `cd` 的话会报 `can't open file 'wreader.cli': No such file or directory` 之类的错。

---

## 第 3 步：安装（一条命令）

在**第 2 步进去的那个 `wreader` 文件夹里**，复制这一行回车：

```bash
./install.sh
```

剩下的它全包了：建虚拟环境 → 装 3 个依赖库 → 把 `werd` 命令**配好别名** → 自检版本号。
中途要下载东西，**需要联网，大约半分钟到几分钟**（看网速）。

跑完最后会打印这样一段：

```
安装完成

命令位置        /Users/你的用户名/wreader/.venv/bin/werd
让别名生效      source "/Users/你的用户名/.zshrc"   （或者干脆关掉终端、重开一个）

接下来
  1. 导入你的书     werd import ~/Downloads/books
  2. 看看书库       werd list
  3. 接着上次读     werd continue   → 把 id 抄给 werd read <id>
```

**照着"让别名生效"那一行再敲一遍**（或者干脆关掉终端、重开一个），就装好了。
（"别名"是什么、为什么需要它，见[关掉终端之后：下次怎么打开 werd](#关掉终端之后下次怎么打开-werd)。）

**验证一下：**

```bash
werd --version
```

输出 `werd 0.1.0` 就成了 🎉 之后**任何新终端窗口**里都能直接敲 `werd`，不用 `cd`、不用激活。

> **几个可能撞上的小问题**
>
> - `./install.sh: Permission denied` → 文件少了"可执行"权限，敲 `chmod +x install.sh` 再重跑。
> - 报 `需要 Python 3.11 或更高版本` → 回到[第 1 步](#第-1-步确认电脑里有-python)装/升级 Python。
> - 想多装点东西 / 不想动配置文件：`./install.sh --dev`（额外装 pytest，要改代码才用得上）、
>   `./install.sh --no-alias`（不碰 `~/.bashrc` / `~/.zshrc`）、`./install.sh --help`（看用法）。
> - **Windows 用户**：`install.sh` 是 macOS / Linux 的脚本，Windows 请走下面的手动步骤。

<details>
<summary><b>想自己手动装？（Windows 用户走这条）</b></summary>

一条一条执行，别一次全粘。

**① 建一个"虚拟环境"**

```bash
python3 -m venv .venv
```

（Windows 如果 `python3` 不行就写 `python -m venv .venv`）

这一步会在当前目录里创建一个叫 `.venv` 的隐藏文件夹，专门给这个项目放依赖库，
不会污染你系统里的 Python。**大约要等十几秒，没有输出就是成功了。**

**② 激活它**

```bash
source .venv/bin/activate          # macOS / Linux
```

Windows（PowerShell）用：

```powershell
.venv\Scripts\activate
```

成功的话，你的命令行提示符前面会多出一个 `(.venv)`，像这样：

```
(.venv) zhangziluo@Mac wreader %
```

**看到 `(.venv)` 就说明进对了。** 这一步是"进入这个项目自带的 Python 环境"。

**③ 安装项目**

```bash
pip install -e .
```

会下载并安装 3 个必需的库（rich、chardet、requests），
需要联网，大概半分钟。最后看到 `Successfully installed ...` 就成功了。

**④ 验证一下**

```bash
werd --version
```

输出 `werd 0.1.0` 就成了 🎉

</details>

> **报 `werd: command not found`？** 两种可能：没激活虚拟环境（回到第 ② 步），
> 或者系统找不到命令位置。不用管，直接把 `werd` 换成 `python -m wreader.cli` 一样能用：
> ```bash
> python -m wreader.cli --version
> ```
> 本指南后面所有 `werd xxx` 都可以这样替换。
>
> 💡 **顺手建议**：手动装的话，每次新开终端窗口都会碰上同样的问题（`werd` 找不到）。
> 花 1 分钟配个别名就能一劳永逸，见[关掉终端之后：下次怎么打开 werd](#关掉终端之后下次怎么打开-werd)。
> （用 `./install.sh` 装的话，这一步已经帮你做好了。）

> **以前装过叫 `nr` 的旧版本？** 不用手动搬数据：第一次运行 `werd` 时，老的 `~/.nr` 目录会被整体搬到
> `~/.wreader`（设置、书库索引、成就状态都在里面），老目录随后消失。如果你以前给终端设过
> `NR_HOME` / `NR_NOVELS_DIR`，它们现在叫 `WREADER_HOME` / `WREADER_NOVELS_DIR`（老名字在没设新名字时仍然生效）。

---

## 第 4 步：准备你的书

把你下载的小说放到一个文件夹里，比如 `~/Downloads/books`。

**支持的格式**：`.txt` 和 `.epub` 两种，别的格式（mobi、azw3、pdf）先转成这两种。
想转 mobi，装个 [Calibre](https://calibre-ebook.com/) 用它转换即可。

**关于文件名**（不强制，但会让书库更整齐）：

wreader 会从文件名里猜书名和作者，支持这几种写法：

| 文件名 | 识别结果 |
| --- | --- |
| `刘慈欣-三体.txt` | 书名《三体》，作者 刘慈欣 ✅ 推荐 |
| `[刘慈欣] 三体.txt` | 书名《三体》，作者 刘慈欣 ✅ |
| `《三体》（校对版全本）作者：刘慈欣.txt` | 书名《三体》，作者 刘慈欣 ✅ 下载站常见命名 |
| `《三体》（修订版）.txt` | 书名《三体》，作者 unknown（括号注释会被去掉） |
| `三体.txt` | 书名《三体》，作者 unknown |

中文书名用 `《》` 包着也行，会被自动去掉；文件名**结尾**的 `（校对版全本）`、`(annotated)` 这类括号注释一律剥掉，
`作者：某人` 会被识别成作者。只有结尾的括号算注释，所以 `书名（中）下册` 这种标题会保持完整。

**关于编码**：不用管。UTF-8（含 BOM）、UTF-16、UTF-32、GBK、GB2312 都能认出来，认不出的会依次尝试 UTF-8 和 GB18030。
如果某本书导入后编码名后面带 `(replaced)`，说明原文件本身有损坏区域（会用 `�` 顶替），变成这样就不会像以前那样
让整次导入中断；把这本用编辑器另存为 UTF-8 再导入就能拿到干净正文。
真的乱码了再看[报错急救表](#报错急救表)。

**关于大小**：单本能导入几十万字的 txt，完全没问题。

---

## 第 5 步：导入书库

把你放书的文件夹告诉 wreader：

```bash
werd import ~/Downloads/books
```

`~` 在终端里代表你的用户主目录，`~/Downloads/books` 就是"下载文件夹里的 books 文件夹"。
也可以直接写完整路径，或者把文件夹拖进终端。

**你会看到类似这样的输出：**

```
imported 2 book(s), skipped 0 duplicate(s), 0 failed
  + ab912556b66b  《Nameless》 unknown · 7 行 · 12 字 · ascii · 2 章
  + 3e027c4de949  《三体》 刘慈欣 · 281 行 · 2000 字 · utf-8 · 41 章
```

逐字解释：

| 部分 | 含义 |
| --- | --- |
| `imported 2 book(s)` | 成功导入了 2 本 |
| `skipped 0 duplicate(s)` | 0 本因为重复被跳过 |
| `0 failed` | 0 本失败 |
| `+ ab912556b66b` | 这本书的 **id**（书的身份证号），**下面要用它来打开书，请复制保存** |
| `《三体》 刘慈欣` | 书名和作者 |
| `281 行 · 2000 字` | 正文总共 281 行、2000 字 |
| `utf-8` | 原文件是什么编码 |
| `41 章` | 识别出 41 个章节 |

如果显示 `no .txt/.epub file found under ...`，说明那个文件夹里没有能被识别的书，检查格式和路径。

**再看一眼书库：**

```bash
werd list
```

```
                              library (2 book(s))
┏━━━┳━━━━━━━━━━━━━━┳━━━━━━━━━━┳━━━━━━━━━┳━━━━━━━━━━┳━━━━━━━┓
┃ # ┃ id           ┃ title    ┃ author  ┃ progress ┃ words ┃
┡━━━╇━━━━━━━━━━━━━━╇━━━━━━━━━━╇━━━━━━━━━╇━━━━━━━━━━╇━━━━━━━┩
│ 1 │ ab912556b66b │ Nameless │ unknown │     0.0% │    12 │
│ 2 │ 3e027c4de949 │ 三体     │ 刘慈欣  │     0.0% │  2000 │
└───┴──────────────┴──────────┴─────────┴──────────┴───────┘
```

`progress` 是阅读进度，`words` 是字数。没读完的书写下一行会继续攒进度。

> **记不住 id 怎么办？** 用 `werd search` 搜出来就行，它的 id 列和 `werd list` 一样：
> ```bash
> werd search 三体
> ```

---

## 第 6 步：读出第一页

```bash
werd read 3e027c4de949
```

（把 id 换成你 `werd list` 里看到的那个）

**忘了上次读到哪本？** 让 wreader 帮你回忆：

```bash
werd continue
```

它会按"最近打开阅读"的时间倒序列出**三本书**（附 id），把 id 抄进 `werd read` 就能接着
上次的位置读。一本都没读过时，它会提示你先用 `werd list` 挑一本。

屏幕会变成这样：

```
★ 第一章 科学边界                ← 最左边这一列是书签栏（有书签的行显示 ★）
 汪淼看到了一串数字在眼前跳动。
 他抬头望向窗外的夜空。
 ...
                                 ← 正文区放多少行取决于终端窗口高度（长段落会自动折行）
21:34 · 第一章 科学边界 · 本章 05:20   ← 状态栏（反色显示）
q退出 j/space翻页 g跳行 [/]章节 Tab目录 /搜索 n下一个 b书签 ?帮助
```

**顶部**是正文，**最左边那一列**是书签栏（有书签的行显示 `★`），
**底部两行**分别是状态栏和提示栏。

**现在试试这个**：

1. 按几次 `空格` —— 往后翻页
2. 按 `k` —— 往回翻
3. 按 `/`，输入一个词（比如主角的名字），回车 —— 跳到第一次出现的地方
4. 按 `n` —— 跳到下一个出现的地方
5. 按 `b` —— 给当前行插个书签
6. 按 `q` —— 退出

退出后你会看到一行总结（书名 · 停在第几行 · 本次读了多久 · 几个书签）：

```
《三体》 · 停在 120/281 行 (42.7%) · 本次 12:30 · 书签 2 个
```

**下次再 `werd read 同一个id`，会从第 120 行继续，不用重新找。**

> ❌ **报错 `werd read needs an interactive terminal`？**
> 说明你在管道或重定向里跑它（比如 `werd read xxx | less`）。
> 阅读器必须在**真正的终端窗口**里运行，不能输出重定向。

---

## 第 7 步：按场景学按键

阅读器里的键不多，按场景记最快。

### 场景 A：我就看中文小说，别的都不想学

只需要 3 个键：`空格`（下一页）、`k`（上一页）、`q`（退出，会自动记住位置）。

顺手再学两个：`/` 搜人名地名，`b` 插书签。

### 场景 B：标记重点

| 按键 | 作用 |
| --- | --- |
| `b` | 当前行加 / 删书签（加过的行最左边会出现 `★`） |
| `g` | 跳到指定行号。会提示 `跳到行号 (1-281):`，输入数字回车 |
| `G` | 直接跳到全书最后一行 |
| `[` | 跳到上一章开头 |
| `]` | 跳到下一章开头 |
| `Tab` | 打开**目录浮层**（屏幕右侧）：`↑↓` 选章、`回车` 跳转、`/` 实时过滤、`q` / `Esc` 关闭 |
| `/` | 搜关键词（中文也能输），回车后跳到第一个命中处 |
| `n` | 下一个命中（到结尾会自动回到第一个） |

### 场景 C：在手机上读（Termux 等）

手机上没有键盘，用**手势**：

- **手指按住往上滑** → 往后读（**一行一行**往下走，不会一下跳过一屏丢掉上下文）
- **手指按住往下滑** → 往回看
- 拖多远就走多少行（拖一行 = 滚一行），所以想看上下文就轻轻滑一点

**调手感**（默认一格滚 1 行）：

```bash
werd config reader.wheel_scroll_step 3    # 改成一格滚 3 行
werd config reader.touch_scroll false     # 不想用拖动手势，只留滚轮和键盘
```

**想用音量键翻页**：去 Termux 的设置里把音量上 / 下映射成 `PageUp` / `PageDown`（或者 `↑` / `↓`），
阅读器本来就认这些键，映射完就能用音量键整页翻。
翻页默认会**上下保留 3 行上下文**（上一屏末尾的 3 行留在新屏幕顶部），所以整页翻也不会一下子跳断；
而且翻页是按**屏幕上看得见的行数**算的 —— 一段几百字不分行的长段落，会被拆成好几屏一屏一屏读完，
不会因为"按文本行跳"而整段跳过去。想改保留几行、或者干脆关掉重叠，用：

```bash
werd config reader.page_overlap 5         # 保留 5 行上下文（默认 3）
werd config reader.page_overlap 0         # 关掉重叠，翻页就是整屏跳转
werd config reader.page_scroll_step 0.5   # 或者改小翻页幅度：半屏一翻
```

> 以前在手机上滑动完全没反应，是因为阅读器**没有开鼠标/触摸上报**，终端根本不会把手势送进来。
> 现在开了；顺带桌面端的鼠标滚轮也能一格一行地滚了。
>
> **如果滑了还是没反应**：说明这个终端的 terminfo 里缺 `XM` 能力，curses 就不会打开
> SGR 鼠标上报。Termux 里可以先试一行：
>
> ```bash
> export TERM=xterm-1006        # 换成带 XM 的 terminfo；想长期生效就写进 ~/.zshrc
> ```
>
> 还不行就退回用「音量键映射」或把 `reader.page_scroll_step` 调小来翻页 —— 只是手感差些，功能不受影响。

### 全部按键总表（可以截图存下来）

| 按键 | 作用 |
| --- | --- |
| `q` / `Q` / `Ctrl-C` | 退出（保存进度、书签、本次阅读时长） |
| `空格` / `j` / `回车` / `↓` / `PageDown` | 下一页 |
| `k` / `↑` / `PageUp` | 上一页 |
| 滚轮 / 触摸拖动 | 逐行滚动（手机上就是靠它） |
| `g` | 跳到行号 |
| `G` | 跳到末尾 |
| `[` / `]` | 上一章 / 下一章 |
| `Tab` | 打开**目录**（右侧浮层）：`↑↓` 选章、`回车` 跳转、`/` 实时过滤、`q` / `Esc` 关闭 |
| `/` | 搜索 |
| `n` | 下一个搜索结果 |
| `b` | 书签开关 |
| `?` | 打开**帮助页**（右侧浮层里列着所有按键，`↑↓` 翻、`q` / `Esc` 关闭；忘光了就按它） |

> 完全记不住也没关系，**底部提示栏一直把这排按键写着呢**，记不住按键就按 `?`。

---

## 第 8 步：看统计和成就

**看看自己读了多少：**

```bash
werd stats
```

```
总阅读时长 12小时34分钟
今日 45分钟 · 本周 5小时12分钟 · 本月 12小时34分钟
每日目标 1小时 · 今日 75% ✓
连续 3 天（每天 ≥30 分钟） · 读完 2 本
最近 30 天（2026-08-23 → 2026-09-21，每列一周，周一开始）
一   ░ · · ▒ ▓
二   · ▒ ░ · █
三   ▒ · · · ·
...
强度：·0 ░<20m ▒<40m ▓<1h █1h+
```

**热力图怎么看**：每一**列**是一周，从上到下是周一到周日；方块越亮表示那天读得越久：

| 符号 | 含义 |
| --- | --- |
| `·` | 那天没读 |
| `░` | 不到 20 分钟 |
| `▒` | 20 分钟 ~ 40 分钟 |
| `▓` | 40 分钟 ~ 1 小时 |
| `█` | 1 小时以上 |

**看看成就解锁进度：**

```bash
werd achievements
```

```
已解锁 1/48
  🏆 🗄️ 书库初成 书库里添加第 1 本书  解锁于 2026-09-21T16:13:43

进行中
  阅读习惯
    ░░░░░░░░░░░░░░  ⏱️ 初窥门径 0分钟/1小时  累计阅读满 1 小时
    ░░░░░░░░░░░░░░  🧱 百日筑基 3/100  连续 100 天打开 werd
    ...
  数据积累
    ██████░░░░░░░░  ✒️ 万字户 4200/10000  累计阅读 1 万字
    ...
```

一共 **48** 个成就，分五类：**阅读习惯**（百日筑基、清晨第一眼、马拉松、超长待机、周末战士、深夜书虫…）、
**数据积累**（第一本、十本大关、万字户…十亿富豪、藏书家…）、
**操作彩蛋**（手速达人、翻页永动机、方向键怀旧、帮助迷）、
**难度挑战**（极限尺寸、窄屏挑战、恢复大师、我反悔），
以及 **10 个隐藏成就**（环游亚欧非美大洋、世界公民、百年世仇、节日读者、名字彩蛋、成就猎人……）——
隐藏的那些在解锁前只显示一个 `❓`，不剧透。

**解锁的那一刻**：

- 如果你正在读书，屏幕**右上角会闪一块牌子**（`🏆 成就解锁 · 成就名`），5 秒后自己消失，
  不挡按键、也不会打断你正在读的那一段；
- 退出阅读器时还会再放一段星星动画 + 横幅，并响一声（嫌吵可以关，见下一步）。

**顺手一试（彩蛋）**：在命令行敲 `werd werd`（或者 `werd word`、`werd --werd`），
有个关于这个名字的小玩笑，顺便解锁「名字彩蛋」。

> 统计是**按阅读时长**算的，退出阅读器时写入。所以刚打开就退出的会话可能显示 0 分钟，这是正常的。
> 一天要读满 30 分钟才算"连续天数"里的一天；不过当天永远算数，因为它正要变成事实。
>
> **少了两个数字？** 早先版本里的"生词数"和"用过翻译次数"已经跟着生词本 / 翻译功能一起退场了：
> `werd stats` 不再显示它们，但 `werd stats --json` 里仍保留 `vocab_count` / `translate_hits` 两个键
> （按 `~/.wreader/vocab.json`、`~/.wreader/cache/` 里的老数据只读统计），老脚本不会因为取不到键而崩。

> **读到一半崩了 / 断电了？** 下次打开**同一本书**时，它会先问一句
> `上次好像没有正常退出 · 上次读到第 N 行`：按 `y` 接着上次的位置读，按别的键从头开始。
> 正常按 `q` 退出过的书不会再问。

---

## 第 9 步：把 wreader 调成你喜欢的样子

所有设置都在一个文件里，**可以直接改文件，也可以用命令改**（推荐命令，会帮你检查拼写和类型）。

```bash
werd config                          # 打印所有设置和默认值
werd config reader.page_height       # 读某项
werd config reader.page_height 30    # 改某项（立刻生效并保存）
werd config --path                   # 想知道设置文件在哪
werd config --reset                  # 全改坏了？一键恢复默认
```

**几个最值得调的东西：**

```bash
# 一屏显示更多行（默认 24，屏幕大的话可以调大）
werd config reader.page_height 32

# 翻页更细腻：每次翻半屏（默认 1 屏）
werd config reader.page_scroll_step 0.5

# 手机上用手指滚动时，一格走几行（默认 1）
werd config reader.wheel_scroll_step 3

# 状态栏显示更多信息（时间 · 书名 · 进度 · 本次时长）
werd config reader.status_bar_format "time|book|percent|elapsed"

# 目标定成每天 90 分钟
werd config stats.daily_goal_minutes 90

# 安静读书：不响铃、不看热力图、不记时长
werd config stats.achievement_sound false
werd config stats.show_heatmap false
werd config reader.store_history false

# 不想让它联网查你在哪（地理成就）：一个 HTTP 请求都不发，完全离线
werd config stats.geo_lookup false

# 书里有内置规则认不出的章节标题？加一条自定义正则
# （多个正则用 | 分隔；内置的「第X章 / Chapter N / 卷X」永远照常生效）
werd config toc.patterns '^### |^第.+回'

# 目录缓存放到别处（留空 = 数据目录下的 cache/，章节解析结果就存在那里）
werd config toc.cache_dir ~/Documents/werd-cache

# 换个地方存小说正文
werd config library.novels_dir ~/Documents/我的小说
```

**状态栏可用的段落共 9 个**（用 `|` 拼起来，写错的名段会被安静忽略）：

`time` 时间 · `book` 书名 · `chapter` 章节 · `position` 行号 · `percent` 百分比 ·
`duration` 本章时长 · `elapsed` 本次时长 · `streak` 连续天数 · `bookmarks` 书签数

设置文件也可以在 Finder / 资源管理器里直接双击改（是个纯文本文件），
`werd config --path` 会告诉你它在哪。**删掉任意一行都会回落到默认值**，改不坏的。

---

## 第 10 步：换电脑或重装，把阅读记录带走

阅读时长、每本书读到哪一行、书签、成就记录——这些**都不在项目文件夹里**，而是住在数据目录
`~/.wreader`（第 8 步提过它）。所以光把项目拷到新电脑是**不够**的，阅读记录要单独搬一次。

### 旧电脑：导出一个数据包

```bash
werd data export ~/werd-data.json
```

```
已导出 2 本书的阅读记录与 7 个成就 → /Users/you/werd-data.json
累计时长 12:00
拷到另一台电脑后跑 werd data import <文件> 即可
```

生出来的 `werd-data.json` 是个纯文本文件，双击就能看懂，不是黑盒备份。
把它拷到新电脑（U 盘 / 网盘 / 微信传文件都行）。

包里装的是**成绩**：累计与每日时长、每本书的进度（行号、百分比、时长）与书签、成就解锁状态。
**不含正文，也不含设置**——正文和设置在新电脑上照第 3～5 步、第 9 步重做一遍就行。

### 新电脑：先装程序、导入书，再合并

```bash
werd import ~/Downloads/books         # 先把同一批书导进来（第 5 步做过就跳过）
werd data import ~/werd-data.json     # 再把时长 / 进度 / 书签 / 成就合并进来
```

```
已合并 2 本书的阅读记录，成就共 7 个
```

⚠️ 四个注意点：

- **书要自己先导一遍**。书的身份是正文的哈希值，正文一样、书号就一样，记录才对得上。
  新电脑上还没有的书会被跳过，并提示一句「先 werd import 再导一次即可补上」
  （全局时长照样接过来，不白跑这一趟）。
- **只加不减，别反复导同一个文件**。时长和事件计数是相加的：同一份包导两遍，时长就算两遍。
  合并只会往里加，不会删掉或改小你已有的记录；一旦某本书标为「读完了」，也不会再被改回未读完。
- **只导入过、一次都没读过的书不进包**（没有进度、时长、书签可搬），所以包里的本数可能比书库少，
  这是正常的。
- **导错文件不会弄坏数据**。文件不存在、语法坏了、根本不是 werd 的数据包，都会打一行
  `error: ...` 并以失败结束，本机数据一个字节都不动。

### 顺手清理：`werd prune` 与 `werd clear`

```bash
werd prune      # 清掉「转好的正文文件已经被你手删掉」的失效书目
werd clear      # 清空书架：书和转好的正文一起删，阅读时长与成就留着
```

- `werd prune` 平时用不上：**每条命令启动前都会自动对一次账**，发现失效书目会打一行提示。
  想显式清一次、或者想看看都少了哪几本时才敲它。
- `werd clear` 忘掉的是「你有哪些书」，不是「你读了多久」。想彻底重来一遍又不想丢统计，用它正合适。
  提醒一句：它会**连转好的正文一起删**（原始电子书在你自己放书的地方，不归 werd 管）。

---

## 关掉终端之后：下次怎么打开 werd

**先说结论**：`werd` 这个命令装在**项目自己的 `.venv` 文件夹**里，
而系统默认的"去哪里找命令"清单（PATH）里没有它。所以你**关掉终端窗口、再开一个新的，
直接敲 `werd` 会报 `command not found`** —— 这不是装坏了，是正常现象，别慌。

> ✅ **如果你是用 `./install.sh` 装的，这一节可以直接跳过**：它已经往 `~/.bashrc` 或 `~/.zshrc`
> 写好别名了（脚本最后一行会告诉你写进了哪个文件、怎么让它立刻生效）。
> 当初加了 `--no-alias`、或者没跑脚本手动装的，再往下看。

有三种办法，**任选一种**（命令里的 `~/Downloads/wreader` 换成你第 2 步里放项目的那个路径）：

| 办法 | 每次要敲什么 | 适合谁 |
| --- | --- | --- |
| ① 配一次**别名**（推荐） | `werd continue` → `werd read <id>` 直接就能用 | 想一劳永逸，以后像普通命令一样用 |
| ② 用**完整路径** | `~/Downloads/wreader/.venv/bin/werd read <id>` | 不想改任何配置文件 |
| ③ 每次**激活虚拟环境** | `cd ~/Downloads/wreader` → `source .venv/bin/activate` → `werd read <id>` | 之后还要在项目里干别的活 |

### 办法①：配一次别名（推荐，一劳永逸）

**只在第一次做一遍。** 把下面这一行复制粘贴进终端，回车：

```bash
echo 'alias werd="$HOME/Downloads/wreader/.venv/bin/werd"' >> ~/.zshrc
```

再让它立刻生效（或者干脆关掉窗口、重新开一个，效果一样）：

```bash
source ~/.zshrc
```

**验证一下：**

```bash
werd --version
```

输出 `werd 0.1.0` 就成了 🎉 以后**任何新终端窗口**里都能直接敲 `werd`，
不用 `cd`、不用激活。

> ⚠️ **四个注意**
> 1. 上面那行命令里的路径要和你的实际项目位置一致，不确定就先看一眼 `pwd`。
> 2. 以后如果把项目文件夹挪走了，记得把 `~/.zshrc` 里那行 `alias werd=...` 的路径一起改掉。
> 3. 用 **bash** 的 Linux 系统，把 `~/.zshrc` 换成 `~/.bashrc`。
> 4. 这行命令重复执行会在文件里多写一行（内容一样，不影响使用）。想知道写了什么，
>    用 `cat ~/.zshrc` 看一眼就行。
>
> **Windows 用户**：PowerShell 的配置文件名不一样，用下面两行代替（存好后重开 PowerShell）：
> ```powershell
> if (!(Test-Path $PROFILE)) { New-Item -ItemType File -Path $PROFILE -Force }
> Add-Content $PROFILE 'function werd { & "$HOME\Downloads\wreader\.venv\Scripts\werd.exe" @args }'
> ```

### 办法②：用完整路径（什么都不用配）

```bash
~/Downloads/wreader/.venv/bin/werd list
~/Downloads/wreader/.venv/bin/werd read 3e027c4de949
```

就是把开头的 `werd` 换成一长串路径，后面完全一样。
Windows 里对应的写法是 `~\Downloads\wreader\.venv\Scripts\werd.exe`。

### 办法③：先激活虚拟环境（就是第 3 步的做法）

```bash
cd ~/Downloads/wreader
source .venv/bin/activate      # Windows: .venv\Scripts\activate
werd read 3e027c4de949
```

提示符前面出现 `(.venv)` 就说明激活好了。注意**激活只对当前这个窗口有效**：
关掉窗口、或者新开一个标签页，都得重做一遍。

> 💡 **别用这一招**：把 `.venv/bin` 加到 PATH 最前面
> （`export PATH="$HOME/Downloads/wreader/.venv/bin:$PATH"`）。
> 那样 `werd` 确实能用了，但会把你**所有**新终端里的 `python3` 和 `pip`
> 都换成这个项目的虚拟环境版本，以后在别的 Python 项目上干活会莫名其妙出错。
> **用别名没有这个副作用**，所以本指南推荐办法①。

### 到底该选哪个？

- 只是**读书**，别的都不管 → 办法①（推荐，配一次就行）或办法②
- 想把 `werd` 和管道、脚本一起用 → 办法①
- 还会进项目目录改代码、跑测试 → 办法③

**好消息**：你的书和进度**跟 `.venv` 没关系**。书库索引、阅读进度、书签、统计都在 `~/.wreader` 里，
所以无论你用哪种办法打开、甚至把 `.venv` 删了重装，**读过的书和进度都不会丢**。

---

## 报错急救表

| 你看到的提示 | 原因 | 怎么办 |
| --- | --- | --- |
| `werd: command not found` | 换了新终端窗口，虚拟环境没激活，或脚本目录不在 PATH。**这是最常见的报错，不代表装坏了** | 用 `./install.sh` 装的：先 `source ~/.zshrc`（bash 换 `~/.bashrc`）或重开终端；否则见[关掉终端之后](#关掉终端之后下次怎么打开-werd)配别名。临时办法：直接用 `~/wreader/.venv/bin/werd`，或先 `cd` 进项目再 `source .venv/bin/activate` |
| `./install.sh: Permission denied` | 脚本没有"可执行"权限（从 ZIP 解压有时会丢掉权限位） | 敲 `chmod +x install.sh` 再重跑 |
| `需要 Python 3.11 或更高版本` | 电脑里的 `python3` 太旧，或者根本没装 | 见[第 1 步](#第-1-步确认电脑里有-python)装 / 升级 Python |
| `can't open file .../wreader.cli: No such file or directory` | 不在项目目录里 | 先 `cd` 到有 `pyproject.toml` 的那个目录 |
| `error: path does not exist: ...` | 路径写错了 | 用 `ls` 确认文件夹存在；路径含空格要加引号 |
| `error: unsupported file type: xxx.mobi (expected .txt or .epub)` | 格式不支持 | 用 Calibre 转成 `.txt` 或 `.epub` |
| `no .txt/.epub file found under ...` | 那个目录里没有书 | 确认后缀是 `.txt` / `.epub`，别是 `.zip` 压缩包 |
| `error: unknown book id: xxx` | 书的 id 写错了 | 跑 `werd list` 或 `werd search 书名` 复制正确的 id |
| `error: werd read needs an interactive terminal (a tty on stdin and stdout)` | 在管道/重定向里跑阅读器 | 直接在终端窗口里跑，别加重定向 |
| `error: the reader needs the curses module` | Windows 缺 curses | `pip install -e ".[windows]"` |
| `error: unknown setting 'reader.pag_height' (did you mean 'reader.page_height'?)` | 设置名拼错了 | 按提示里给的建议名重写；`werd config` 可以看到全部合法名 |
| `error: 'reader.page_height' expects an integer, got 'abc'` | 类型不对 | 数字项写数字，布尔项写 `true` / `false` |
| `warning: unknown setting 'translator.backend' is ignored` | `settings.toml` 里留着有翻译功能时代的旧键（`translator.*` / `translate.*` / `vocab.*`） | 无害，只是提醒；想清理就 `werd config --reset` 回到默认，或手动删掉那几行 |
| `error: unknown setting 'translator.backend'` | 想读 / 改一个**已经不存在**的设置 | 翻译与生词本设置已随功能一起移除；用 `werd config` 看当前全部合法设置名 |
| `error: <文件> is not valid JSON` / `is not valid TOML` | 数据文件被手工改坏了 | 用编辑器检查语法（引号、逗号）；实在不行删掉让它重新生成 |
| 中文显示成一堆方块/问号 | 终端字体或缺 UTF-8 支持 | macOS 用自带终端或 iTerm2；Windows 用 Windows Terminal；把终端编码设成 UTF-8 |
| 导入后正文是乱码 | 源文件编码极端异常，或原文件本身损坏 | 看导入输出里的编码名；带 `(replaced)` 说明原文件有损坏区域，用编辑器另存为 UTF-8 后重新 `werd import`（或直接删掉这本） |
| `pip install -e .` 卡住或失败 | 网络问题 / pip 太旧 | 先 `python -m pip install --upgrade pip` 再重试；国内可加镜像 `-i https://pypi.tuna.tsinghua.edu.cn/simple` |
| 退出后统计没增加 | 关了统计，或数据目录不可写 | `werd config reader.store_history` 应为 `true`；确认 `~/.wreader` 目录有写权限 |
| `error: no such data file: xxx` | `werd data import` 给的路径不存在（或把目录当成了文件） | 用 `ls` 确认文件真在那儿；导出的包是个**文件**，不是文件夹 |
| `error: xxx is not a werd data file` | 拿别的 JSON（比如 `library.json`）当数据包导入了 | 换成 `werd data export` 生成的那个文件；被拒的导入**不会**动本机数据 |
| `error: xxx was written by another version (bundle 2, expected 1)` | 包的格式比当前程序新 | 把两边都升级到同一版本，或在新版本上重新 `werd data export` 一次 |
| `werd data import` 说有几本书被跳过 | 新机器上还没导入这些书（书的身份是正文的哈希值，书不在就没地方挂记录） | 先 `werd import ~/Downloads/books` 把书补上，再跑一次 `werd data import`；全局时长已经接过来了，不会白导 |
| 统计里不再有"生词 128 个"这类数字 | 生词本与翻译功能已从阅读器移除（相关成就仍在只读老数据） | 想继续看老数字用 `werd stats --json` 里的 `vocab_count` / `translate_hits`；新版本不再产生新数据 |

**万能排错三连**：

```bash
werd config --path        # 数据文件在哪
werd config               # 设置有没有改坏
werd import 出问题的文件夹  # 重新扫描一遍
```

---

## 常用命令速查卡

```bash
# —— 安装（只需一次）——
git clone https://github.com/zhangziluo/wreader.git    # ① 下载到 ./wreader
cd wreader && ./install.sh                             # ② 一键装好（venv + 依赖 + 别名）
# 不想用脚本 / Windows：python3 -m venv .venv && source .venv/bin/activate && pip install -e .

# —— 新开了终端窗口，先让 werd 能用（任选一种，见上方"关掉终端之后"一节）——
werd list                                   # ① 配过别名：直接就能用
~/Downloads/wreader/.venv/bin/werd list     # ② 没配别名：换成完整路径
cd ~/Downloads/wreader && source .venv/bin/activate   # ③ 或先激活虚拟环境再敲 werd

# —— 日常使用 ——
werd import ~/Downloads/books               # 导入（可重复跑，自动跳过重复的书）
werd list                                   # 书库列表（拿 id）
werd search 关键词                          # 模糊搜索（书名/作者/标签，支持 hptr 这种首字母）
werd read 3e027c4de949                      # 开读
werd continue                               # 最近打开阅读的三本书（拿 id 接着读）
werd toc 3e027c4de949                       # 看这本书的目录（章节 + 进度%）；--rebuild 强制重建

# —— 阅读器里 ——
# 空格/j 下一页   k 上一页   q 退出    / 搜索   n 下一个   b 书签
# g 跳行   G 末尾   [ ] 上下章   Tab 目录   ? 帮助页（忘了按键就按它）

# —— 统计与成就 ——
werd stats                                  # 时长 + 热力图
werd stats --json                           # 给脚本用的原始数据
werd achievements                           # 成就进度

# —— 数据搬家与清理 ——
werd data export ~/werd-data.json           # 导出时长/进度/书签/成就（换电脑用，见第 10 步）
werd data import ~/werd-data.json           # 在新电脑上合并回来（只加不减，别反复导）
werd prune                                  # 清掉正文文件已被删掉的失效书目
werd clear                                  # 清空书架（正文一起删；阅读时长与成就保留）

# —— 设置 ——
werd config                                 # 看全部设置
werd config 键 值                           # 改一项，如 werd config reader.page_height 32
werd config --path                          # 设置文件在哪
werd config --reset                         # 恢复默认
```

---

## 想卸载怎么办

wreader 不会往系统里乱装东西，数据也都集中在两个地方，删干净很容易：

```bash
# 1. 删掉程序本体（虚拟环境）
cd /Users/你的用户名/Downloads/wreader
rm -rf .venv

# 2. 删掉所有阅读数据（书库索引 / 设置 / 统计与成就 / 目录缓存）
rm -rf ~/.wreader

# 3. 删掉转好的小说正文（注意：如果你把别的书也放在这里，请自行挑选）
rm -rf ~/novels
```

只想**清空阅读记录、书留着**的话，只跑第 2 步即可（会一并清掉统计、成就状态、书签和目录缓存）。

- 只想**清空书架、统计留着**：敲 `werd clear`（删书 + 删转好的正文，时长与成就保留）。
- 只想把**阅读成绩搬到新电脑**、不想搬整个数据目录：`werd data export` 出一个 JSON 包，
  在新机器上 `werd data import` 合并回来（详见[第 10 步](#第-10-步换电脑或重装把阅读记录带走)）。

想**备份**的话，把 `~/.wreader` 整个文件夹复制走就行——所有记录都在里面，换电脑拷过去就能接着读；
只想要成绩部分的话，第 10 步的 `werd data export` 一个文件就够了。

---

## 术语小词典

| 名词 | 通俗解释 |
| --- | --- |
| **终端 / Terminal** | 那个只能打字、黑底白字的窗口。所有命令都在这里敲 |
| **命令行 / CLI** | 用敲命令代替点鼠标的操作方式 |
| **目录 / 文件夹** | 一回事，`~` 代表你的用户主目录 |
| **路径** | 一个文件或文件夹的位置，比如 `~/Downloads/books` |
| **虚拟环境（venv）** | 给项目单独准备的"隔离沙盒"，装的库不会影响系统里的其他程序。激活后提示符前会出现 `(.venv)` |
| **一键安装脚本（install.sh）** | 项目自带的小脚本，把"建 venv + 装依赖 + 配别名"一次做完。不放心可以用记事本打开看看它干了什么 —— 两百来行，全是普通命令 |
| **别名（alias）** | 给一长串命令起个短名字。比如把 `~/Downloads/wreader/.venv/bin/werd` 起名为 `werd`，之后敲 `werd` 就等于敲那一长串。详见[关掉终端之后](#关掉终端之后下次怎么打开-werd) |
| **PATH** | 系统"该去哪里找命令"的清单。不在清单里的命令，敲了就会报 `command not found`。werd 装在项目的 `.venv` 里，默认不在这份清单里 |
| **依赖 / 依赖库** | 程序运行需要借助的别人写好的功能包，比如 rich 负责画表格 |
| **pip** | Python 的"应用商店"，`pip install xxx` 就是安装 xxx |
| **`python -m wreader.cli`** | "用 Python 运行 wreader.cli 这个模块"，是没有 `werd` 命令时的等价写法 |
| **id（书号）** | 每本书导入时生成的 12 位编号，像身份证号，用来准确指认一本书 |
| **txt / epub** | 两种电子书格式。txt 是纯文本，epub 是带排版的电子书 |
| **编码（UTF-8 / GBK）** | 文字存成文件时的"密码本"，对不上就会乱码。wreader 会自动处理 |
| **真终端（tty）** | 能接收键盘实时输入、能刷屏的终端。阅读器必须在这种环境里跑 |
| **缓存** | 已经算好的结果先存起来，下次直接用，不再重复算（比如目录缓存） |
| **热力图** | 用方块深浅表示"哪天读了多少"的小图表 |
| **连续天数** | 连着多少天都达到了最低阅读门槛（每天 30 分钟） |
| **成就** | 达成某个小目标后弹出的徽章，纯属自娱自乐的正反馈 |

---

**相关文档**：[README.md](README.md) —— 完整的命令参考、设置项清单、数据格式说明和项目结构。
English readers: [README.en.md](README.en.md).




